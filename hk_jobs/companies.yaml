##############################################################################
#  ⚠  UNVERIFIED ATS ASSIGNMENTS — READ BEFORE RUNNING  ⚠
#
#  Every `adapter:` value and every config value marked "# TODO" below is
#  an educated guess. NONE have been verified against live careers pages.
#
#  HOW TO VERIFY A SINGLE COMPANY (takes ~2 minutes per company):
#    1. Open the company's careers page in Chrome.
#    2. DevTools  →  Network tab  →  tick "Fetch/XHR".
#    3. Type in the job-search box or scroll the listing.
#    4. Look at the request URLs that appear:
#         • contains "myworkdayjobs.com"  →  adapter: workday
#             URL path: /wday/cxs/{tenant}/{site}/jobs  →  note tenant + site
#             Subdomain prefix (wd1/wd3/wd5) is the `wd:` value
#         • contains "eightfold.ai"       →  adapter: eightfold
#             Subdomain: {tenant}.eightfold.ai  →  note tenant
#             "domain" query param in the XHR call  →  note domain
#         • contains "taleo.net"          →  adapter: jobsdb  (Taleo is hostile)
#         • contains "icims.com"          →  adapter: jobsdb  (iCIMS is hostile)
#         • no ATS XHR found             →  adapter: jobsdb  (fallback)
#    5. For the JobsDB slug: visit hk.jobsdb.com, search the company,
#       click their profile. The slug is the part before "-jobs" in the URL,
#       e.g. hk.jobsdb.com/bank-of-china-hong-kong-jobs → "bank-of-china-hong-kong".
#
#  Remove the "# TODO verify" comment once a value has been confirmed.
#  Set `enabled: false` to pause a company without deleting its config.
##############################################################################

# Schema:
#   name    : human-readable company name
#   slug    : kebab-case identifier — used as company_slug in Job objects
#   adapter : workday | eightfold | jobsdb
#   enabled : true | false
#   config  :
#     workday   → tenant (str, TODO), site (str, TODO), wd (str, default "wd3"),
#                 location_filter (str, default "Hong Kong")
#     eightfold → tenant (str, TODO), domain (str, TODO),
#                 location_filter (str, default "Hong Kong")
#     jobsdb    → jobsdb_slug (str), max_pages (int, default 5),
#                 use_company_profile (bool, default true) — true scrapes
#                   /{slug}-jobs/at-this-company (employer-verified listings only);
#                   false scrapes /{slug}-jobs (broader listing, may include agents).
#                 proxy (str, optional) — residential proxy URL to bypass Cloudflare.
#                 Format: "http://user:pass@host:port"
#                 Services: Bright Data (brightdata.com), Oxylabs, Smartproxy, IPRoyal.
#                 Omit if you have direct access; required for most CI/datacenter IPs.
#                 ⚠ Do NOT commit real credentials — use an env var or secrets manager:
#                   proxy: !ENV HK_JOBS_PROXY   (requires a custom YAML loader)

companies:

  # ── Banking ──────────────────────────────────────────────────────────────
  # Source: https://www.hsbc.com.hk, https://www.bochk.com, etc.

  - name: HSBC Hong Kong
    slug: hsbc-hk
    adapter: eightfold           # hsbc.eightfold.ai confirmed by user
    enabled: true
    config:
      tenant: hsbc               # TODO verify: open hsbc.eightfold.ai in DevTools
      domain: hsbc.com           # TODO verify: check "domain" query param in XHR

  - name: Bank of China (Hong Kong)
    slug: bochk
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Bank-of-China-(Hong-Kong)
      use_company_profile: true
      max_pages: 10   # known-big firm: override the global default of 6 to keep full coverage
      # proxy: "http://user:pass@residential-proxy.example.com:8080"  # uncomment + fill in

  - name: Standard Chartered Bank (HK)
    slug: standard-chartered-hk
    adapter: jobsdb              # switched from workday: 422 on /wday/cxs/standardchartered/External/jobs
    enabled: true
    config:
      jobsdb_slug: Standard-Chartered   # verified slug format

  - name: Hang Seng Bank
    slug: hang-seng-bank
    adapter: eightfold
    enabled: false               # DISABLED: Hang Seng jobs are already included in the HSBC HK result.
                                 # Both share hsbc.eightfold.ai, but domain=hangseng.com returns 404 —
                                 # Hang Seng jobs appear to be registered under hsbc.com in Eightfold.
                                 # Re-enable only if you need Hang Seng results separated from HSBC.
    config:
      tenant: hsbc
      domain: hangseng.com

  - name: DBS Bank (Hong Kong)
    slug: dbs-hk
    adapter: jobsdb
    enabled: false               # TODO: source via eFinancialCareers
    config:
      jobsdb_slug: DBS-Bank

  - name: Bank of East Asia
    slug: bank-of-east-asia
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: The-Bank-of-East-Asia
      use_company_profile: true
      max_pages: 10   # known-big firm: override the global default of 6 to keep full coverage

  - name: Citibank Hong Kong
    slug: citibank-hk
    adapter: jobsdb              # Citi uses Taleo (hostile) globally; JobsDB fallback
    enabled: true
    config:
      jobsdb_slug: Citibank
      use_company_profile: true
      max_pages: 10   # known-big firm: override the global default of 6 to keep full coverage

  - name: ICBC (Asia)
    slug: icbc-asia
    adapter: jobsdb              # Chinese state bank; direct ATS unknown
    enabled: true
    config:
      jobsdb_slug: Industrial-&-Commercial-Bank-of-China
      use_company_profile: true

  - name: China Construction Bank (Asia)
    slug: ccb-asia
    adapter: jobsdb              # Chinese state bank; direct ATS unknown
    enabled: true
    config:
      jobsdb_slug: china-construction-bank-asia   # TODO verify JobsDB slug

  - name: OCBC Wing Hang Bank
    slug: ocbc-wing-hang
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: OCBC-Bank
      use_company_profile: true

  # ── Insurance ────────────────────────────────────────────────────────────
  # Source: https://www.aia.com.hk, https://www.manulife.com.hk, etc.

  - name: AIA Hong Kong
    slug: aia-hk
    adapter: workday             # CONFIRMED: aia.wd3.myworkdayjobs.com
    enabled: true
    config:
      tenant: aia
      site: External
      wd: wd3

  - name: Manulife Hong Kong
    slug: manulife-hk
    adapter: jobsdb              # Workday tenant manulife.wd1 exists but site name unknown; JobsDB fallback
    enabled: true
    config:
      jobsdb_slug: Manulife      # verified: hk.jobsdb.com/Manulife-jobs
      max_pages: 10   # known-big firm: override the global default of 6 to keep full coverage

  - name: Prudential Hong Kong
    slug: prudential-hk
    adapter: workday             # verified: prudential.wd3.myworkdayjobs.com, site=prudential (524 total jobs)
    enabled: true
    config:
      tenant: prudential
      site: prudential
      wd: wd3

  - name: AXA Hong Kong
    slug: axa-hk
    adapter: jobsdb              # direct ATS not confirmed; JobsDB fallback
    enabled: true
    config:
      jobsdb_slug: axa
      use_company_profile: true
      max_pages: 10   # known-big firm: override the global default of 6 to keep full coverage

  - name: Zurich Hong Kong
    slug: zurich-hk
    adapter: jobsdb              # switched from workday: 422 on /wday/cxs/zurich/External/jobs
    enabled: true
    config:
      jobsdb_slug: Zurich-Insurance-Group
      use_company_profile: true

  - name: FWD Insurance
    slug: fwd-insurance
    adapter: workday             # verified: fwd.wd3.myworkdayjobs.com/FWDcareersite (187 total jobs)
    enabled: true
    config:
      tenant: fwd
      site: FWDcareersite
      wd: wd3

  - name: Sun Life Hong Kong
    slug: sun-life-hk
    adapter: workday             # verified: sunlife.wd3.myworkdayjobs.com/Experienced-Jobs (278 total jobs)
    enabled: true
    config:
      tenant: sunlife
      site: Experienced-Jobs
      wd: wd3

  - name: HSBC Life
    slug: hsbc-life
    adapter: eightfold
    enabled: false               # DISABLED: HK roles already captured under HSBC HK (domain=hsbc.com).
                                 # Verified via Eightfold API: 198 "HSBC Life" branded jobs exist but
                                 # are overwhelmingly Singapore-based. The 46 HK matches (underwriting,
                                 # insurance roles) appear in the HSBC HK 239-job result. No separate
                                 # HK-specific Eightfold domain exists for HSBC Life.
    config:
      tenant: hsbc
      domain: hsbclife.com       # moot — not used while disabled

  - name: Generali Hong Kong
    slug: generali-hk
    adapter: jobsdb              # direct ATS not identified; JobsDB fallback
    enabled: true
    config:
      jobsdb_slug: generali-life   # verified slug

  - name: China Taiping Insurance
    slug: china-taiping
    adapter: jobsdb              # Chinese insurer; direct ATS unknown
    enabled: true
    config:
      jobsdb_slug: China-Taiping-Life-Insurance-(Hong-Kong)-Company-Limited
      use_company_profile: true

  # ── Asset Management ──────────────────────────────────────────────────────
  # Source: https://www.valuepartners-group.com, https://www.fidelity.hk, etc.

  - name: Value Partners
    slug: value-partners
    adapter: jobsdb              # HK-listed boutique; no major ATS identified
    enabled: true
    config:
      jobsdb_slug: Value-Partners   # verified slug
      max_pages: 10   # known-big firm: override the global default of 6 to keep full coverage

  - name: Fidelity International HK
    slug: fidelity-international-hk
    adapter: jobsdb              # switched from workday: 422 on /wday/cxs/fidelityinternational/External/jobs
    enabled: true
    config:
      jobsdb_slug: Fidelity-(Far-East)-Trading
      use_company_profile: true

  - name: BlackRock Hong Kong
    slug: blackrock-hk
    adapter: jobsdb              # switched from workday: 422 on /wday/cxs/blackrock/Experienced/jobs
    enabled: true
    config:
      jobsdb_slug: blackrock     # TODO verify JobsDB slug

  - name: Schroders Hong Kong
    slug: schroders-hk
    adapter: jobsdb              # switched from workday: 422 on /wday/cxs/schroders/External/jobs
    enabled: true
    config:
      jobsdb_slug: schroders     # TODO verify JobsDB slug

  - name: Amundi Hong Kong
    slug: amundi-hk
    adapter: jobsdb
    enabled: false               # TODO: source via eFinancialCareers
    config:
      jobsdb_slug: amundi-hong-kong

  - name: Man Group Hong Kong
    slug: man-group-hk
    adapter: jobsdb
    enabled: false               # TODO: source via eFinancialCareers
    config:
      jobsdb_slug: man-group

  - name: PIMCO Hong Kong
    slug: pimco-hk
    adapter: jobsdb              # switched from workday: 422 on /wday/cxs/pimco/External/jobs
    enabled: true
    config:
      jobsdb_slug: pimco         # TODO verify JobsDB slug

  - name: UBS Asset Management HK
    slug: ubs-am-hk
    adapter: jobsdb              # switched from workday: 422 on /wday/cxs/ubs/External/jobs
    enabled: true
    config:
      jobsdb_slug: ubs           # TODO verify JobsDB slug

  - name: BNP Paribas Asset Management HK
    slug: bnp-paribas-am-hk
    adapter: jobsdb              # direct ATS not confirmed; JobsDB fallback
    enabled: true
    config:
      jobsdb_slug: bnp-paribas-asset-management   # TODO verify JobsDB slug

  ##############################################################################
  #  2026-06 EXPANSION — onboarded via scripts/discover_companies.py
  #  All confirmed reachable on JobsDB (HK_Job_Count > 0 on a real company page;
  #  a nonsense slug returns 0, so these are validated pages, not fuzzy search).
  #  See scripts/discovered_companies.csv for the full probe record.
  #  NOTE: Western investment banks (Goldman, JPMorgan, BofA, Deutsche, Morgan
  #  Stanley, Barclays) may include third-party/agency reposts under the brand
  #  page — disable individually if the scraped data proves low-quality.
  ##############################################################################

  # ── Expansion: Mainland China / Asian banks ───────────────────────────────
  - name: Agricultural Bank of China
    slug: agricultural-bank-china
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Agricultural-Bank-of-China

  - name: Bank of Communications
    slug: bank-of-communications
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Bank-of-Communications

  - name: China CITIC Bank
    slug: citic-bank
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: China-CITIC-Bank-International-Limited
      use_company_profile: false    # no /at-this-company profile; use plain listing

  - name: China Everbright Bank
    slug: everbright-bank
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: China-Everbright-Bank

  - name: China Merchants Bank
    slug: china-merchants-bank
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: China-Merchants-Bank

  - name: China Minsheng Bank
    slug: minsheng-bank
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: China-Minsheng-Bank

  - name: Postal Savings Bank of China
    slug: psbc
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Postal-Savings-Bank-of-China

  - name: Mitsubishi UFJ (MUFG)
    slug: mufg
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: MUFG-Bank
      use_company_profile: true

  - name: Mizuho Financial Group
    slug: mizuho
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Mizuho-Bank
      use_company_profile: true

  - name: SMBC Group
    slug: smbc
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Sumitomo-Mitsui-Banking-Corporation
      use_company_profile: true

  - name: ANZ Group
    slug: anz
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: ANZ-Group

  - name: UOB
    slug: uob
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: United-Overseas-Bank
      use_company_profile: true

  - name: Maybank
    slug: maybank
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Maybank

  - name: State Bank of India
    slug: sbi
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: State-Bank-of-India

  # ── Expansion: Western investment banks (see agency-repost note above) ─────
  - name: Bank of America
    slug: bank-of-america
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Bank-of-America

  - name: Deutsche Bank
    slug: deutsche-bank
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Deutsche-Bank

  - name: Goldman Sachs
    slug: goldman-sachs
    adapter: jobsdb
    enabled: false               # TODO: source via eFinancialCareers
    config:
      jobsdb_slug: Goldman-Sachs

  - name: JPMorgan
    slug: jpmorgan
    adapter: jobsdb              # covers both bank and asset management (merged)
    enabled: true
    config:
      jobsdb_slug: JPMorganChase
      use_company_profile: true

  - name: Morgan Stanley
    slug: morgan-stanley
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Morgan-Stanley

  - name: Barclays
    slug: barclays
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Barclays

  # ── Expansion: Insurance ───────────────────────────────────────────────────
  - name: China Life Insurance
    slug: china-life
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: China-Life-Insurance

  - name: China Pacific Insurance
    slug: china-pacific-insurance
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: China-Pacific-Life-Insurance
      use_company_profile: true

  - name: Ping An Insurance
    slug: ping-an
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Ping-An-Insurance

  - name: Chubb
    slug: chubb
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Chubb-Insurance
      use_company_profile: true

  - name: Allianz
    slug: allianz
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Allianz-Worldwide-Partners
      use_company_profile: true

  - name: Swiss Re
    slug: swiss-re
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Swiss-Re

  - name: Samsung Life Insurance
    slug: samsung-life
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Samsung-Life-Insurance

  - name: MetLife
    slug: metlife
    adapter: jobsdb
    enabled: false               # TODO: source via eFinancialCareers
    config:
      jobsdb_slug: MetLife

  - name: Nippon Life Insurance
    slug: nippon-life
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Nippon-Life-Insurance

  # ── Expansion: Asset management ────────────────────────────────────────────
  - name: Macquarie Group
    slug: macquarie
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Macquarie-Group

  - name: State Street Global Advisors
    slug: state-street
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: State-Street-Global-Advisors

  - name: Invesco
    slug: invesco
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Invesco-Hong-Kong-Limited
      use_company_profile: true

  - name: Northern Trust
    slug: northern-trust
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: Northern-Trust

  - name: KKR
    slug: kkr
    adapter: jobsdb
    enabled: true
    config:
      jobsdb_slug: KKR
      use_company_profile: false    # no /at-this-company profile; use plain listing

  ##############################################################################
  #  2026-06 EXPANSION — DEAD ENDS (probed, no reachable HK jobs)
  #  Returned 0 on JobsDB under the probed slug AND on Workday/Eightfold tenant
  #  guesses. Kept here (enabled: false) so we don't re-investigate them. Several
  #  likely DO have HK offices but on their own ATS / a different JobsDB slug —
  #  re-enable with the correct jobsdb_slug if you find one.
  ##############################################################################
  - name: Apollo Global Management
    slug: apollo
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: Apollo-Global-Management

  - name: Brookfield Asset Management
    slug: brookfield
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: Brookfield-Asset-Management

  - name: Carlyle Group
    slug: carlyle
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: Carlyle-Group

  - name: GIC Private Limited
    slug: gic
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: GIC-Private-Limited

  - name: KB Financial Group
    slug: kb-financial
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: KB-Financial-Group

  - name: Munich Re
    slug: munich-re
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: Munich-Re

  - name: Shinhan Financial Group
    slug: shinhan
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: Shinhan-Financial-Group

  - name: Societe Generale
    slug: societe-generale
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: Societe-Generale

  - name: T. Rowe Price
    slug: t-rowe-price
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: T-Rowe-Price

  - name: Temasek Holdings
    slug: temasek
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: Temasek-Holdings

  - name: Tokio Marine Holdings
    slug: tokio-marine
    adapter: jobsdb
    enabled: false   # no HK JobsDB page under probed slug as of 2026-06
    config:
      jobsdb_slug: Tokio-Marine-Holdings
