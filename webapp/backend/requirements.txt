fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
# Backs pydantic's EmailStr, used to validate addresses on /api/contact and
# /api/post-role. Pydantic raises at import time without it.
email-validator==2.3.0
# Argon2id password hashing for Seeker accounts (webapp/backend/auth.py, ADR 0004).
# Version resolved by installing it and reading back what pip actually put in the
# venv — a guessed pin has broken a build in this repo before. argon2-cffi pulls
# in argon2-cffi-bindings (the compiled reference implementation), pinned here too
# so the build is reproducible rather than "whatever resolved today".
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
# The Google OAuth code/token exchange (webapp/backend/main.py, PLAN_ACCOUNTS
# phase 4). Was already present transitively via starlette's TestClient; now a
# real production dependency, so it is pinned explicitly rather than left to
# whatever a test-only transitive resolves to. Matches the repo's decided HTTP
# client (CLAUDE.md: httpx, not requests) — hk_jobs already depends on it.
httpx==0.28.1
# Backs RedisRateLimiter (webapp/backend/rate_limit.py) — the shared rate
# limiter used once Settings.redis_url is set (more than one replica, or
# taking payment; see rate_limit.py's module docstring). Optional at runtime:
# with REDIS_URL unset the app never imports this and keeps the in-process
# RateLimiter, so a deploy with no Redis attached is unaffected.
redis==8.1.0
