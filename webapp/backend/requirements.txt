fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
# Backs pydantic's EmailStr, used to validate addresses on /api/contact and
# /api/post-role. Pydantic raises at import time without it.
email-validator==2.3.0
