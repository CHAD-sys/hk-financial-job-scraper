# Frontend build-time env (set in Railway → frontend service → Variables).
# Copy to .env.local for local dev. Vite only exposes VITE_-prefixed vars.

# Base URL of the deployed backend API (no trailing slash).
# Example: https://your-backend.up.railway.app
VITE_API_URL=http://localhost:8000
